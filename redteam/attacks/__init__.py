"""Attack payload libraries, grouped by category.

Payloads are data, not test logic — kept here so a new attack variant is a
one-line addition to a list, not a new test function. Each list is small and
purpose-built for *this* application (a scoped personal-finance assistant
with a strict system prompt, no tool-calling, and OCR-sourced transaction
data) rather than a generic jailbreak benchmark dump.
"""
