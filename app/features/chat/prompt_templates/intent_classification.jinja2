Classify the intent of the LATEST user message into one of: analysis, planning, recommendation, general. Use the recent conversation for context if the latest message alone is ambiguous.

If the message describes, asks for help with, or seeks a method for an illegal or harmful act (e.g. theft, fraud, money laundering, violence, evading law enforcement) — even if it uses financial vocabulary like "bank" or "money" — classify it as general, never as planning, analysis, or recommendation.
{% if history %}Recent conversation:
{{ history }}

{% endif %}Latest user message: {{ message }}
Respond with ONLY the intent word for the latest message.
