Generate a monthly budget allocation as percentages summing to exactly 100. User's average monthly income: {{ avg_monthly_income }}. Known recurring expenses: {{ avg_monthly_recurring_expense }}. Known savings goal from their profile: {{ savings_goal_name }} (target {{ savings_goal_target_amount }}, within {{ savings_goal_timeline_months }} months). Questionnaire's savings_goal answer: {{ savings_goal_answer }}. If these describe the same goal, don't double-count it; if they're different, treat both as goals to weigh when allocating savings. Questionnaire answers: {{ answers }}. Return ONLY a JSON object mapping category names to integer percentages, using ONLY these category names: {{ known_categories | join(', ') }}. Example: {{ '{' }}{{ example_categories }}{{ '}' }}